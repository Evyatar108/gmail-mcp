# Skills

- `gmail-mcp` — safe Gmail tool usage.
- `gmail-mcp-setup` — installation, OAuth, sharing, and diagnostics.
- `gmail-inbox-organizer` — iterative metadata-first Inbox cleanup.

Install all public Gmail skills:

```powershell
.\scripts\install-user-skill.ps1
```
